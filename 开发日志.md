# Life OS 开发日志

> 记录每次迭代的改动、原因与验证情况。最新的在最上面。

---

## 2026-07-04 · 嵌入向量笔记检索 + APK 编译成功

### 本次目标
为 AI 助手增加「搜笔记」能力：保存日记/感悟时自动生成向量，AI 对话时检索相关笔记注入上下文。

### 已完成

#### 1. 嵌入向量模块 (`src/utils/embeddings.js` 新增)
- `embedNote(note, settings)` — 单条笔记生成向量并缓存到 AsyncStorage
- `searchNotes(query, allNotes, settings)` — 余弦相似度检索 + 重排序 + topK
- `fetchEmbedding(input, settings)` — 调用 OpenAI 兼容 embedding API
- `deleteEmbedding(noteId)` / `clearEmbeddings()` — 缓存管理
- `loadEmbeddings()` / `saveEmbeddings()` — AsyncStorage 持久化
- `formatNotesContext(results)` — 格式化检索结果给 LLM

#### 2. 通用工具函数 (`src/utils/helpers.js` 已修改)
- 新增：`cosineSimilarity`, `rerankNotes`, `noteForEmbedding`, `allNotesFromState`, `getEmbeddingConfig`, `getLLMConfig`

#### 3. 保存时自动 embed (`src/components/AddNoteSheet.js` 已修改)
- `handleSave` 新增 `id/type/createdAt/updatedAt` 字段
- 保存后后台异步调用 `embedNote(payload, state.settings)`
- 失败只 `console.warn`，不阻塞 UI

#### 4. 删除时同步删向量 (`src/screens/LearnScreen.js` 已修改)
- `handleDelete` 中调用 `deleteEmbedding(item.id)`

#### 5. AI 助手接入笔记检索 (`src/components/AIChatSheet.js` 已修改)
- props 新增 `diary` 和 `insights`
- `sendToAI` 中先用 `inferNoteQuery()` 判断是否需要检索
- 需要则调 `searchNotes` → `formatNotesContext` → 注入 system prompt
- 新增 `search_notes` 工具定义

#### 6. App.js 传入 diary/insights
- `<AIChatSheet ... diary={state.diary} insights={state.insights} ... />`

#### 7. constants 新增嵌入模型配置
- `STORAGE_KEYS.embeddings`
- `embeddingApiKey`/`embeddingBaseUrl`/`embeddingModel` 字段

### APK 编译
- ✅ **BUILD SUCCESSFUL** in 4m 8s（352 tasks）
- APK: `D:\lifeosbuild\LifeOSApp\android\app\build\outputs\apk\release\app-release.apk` (69.9 MB)
- node 路径问题已解决：用 `D:\apps\nodebin\` 无空格路径
- Metro Bundler 成功打包 841 modules (24.67s)
- `react-native-markdown-display` 和 `punycode` polyfill 已在 node_modules 中

### 阿里云百炼嵌入模型推荐
根据搜索结果，阿里云百炼（DashScope）当前推荐：
- **text-embedding-v4** — 最新最强，MTEB 榜单超越 OpenAI/Google
- **text-embedding-v3** — 1536 维，稳定可靠
- **Qwen3-Embedding-8B/4B/0.6B** — 2025 新系列，中文 RAG 最优
- API 兼容 OpenAI 格式，Base URL: `https://dashscope.aliyuncs.com/compatible-mode/v1`
- 新用户免费 7000 万 tokens

### 已添加阿里云模型到预设
- **LLM_PRESETS** 新增：阿里云 通义千问 Plus/Turbo/Max
- **EMBEDDING_PRESETS** 新增：阿里云 text-embedding-v4/v3
- 删除了不靠谱的 DeepSeek Embedding 预设（DeepSeek 无 embedding API）
- 二次编译成功（23s），APK 已更新

### 待办
- [ ] APK 传输到手机实测
- [ ] `embedding` state 展示 UI 提示（「正在索引…」）
- [ ] SettingsScreen 加批量 embed 按钮
- [ ] SettingsScreen 嵌入模型配置 UI 确认

---

## 2026-07-04 · 代码审查 + 阿里云百炼预设 + 二次编译

### 代码审查结论
逐个审查了所有关键文件，代码状态良好：
- ✅ `AIChatSheet.js` — Markdown 渲染 + 工具调用 + 笔记检索链路完整
- ✅ `embeddings.js` — 本地优先向量检索模块完整
- ✅ `helpers.js` — 所有工具函数就绪（cosineSimilarity/rerankNotes 等）
- ✅ `SettingsScreen.js` — 折叠式设置页，语言模型 + 嵌入模型配置 UI 已就绪
- ✅ `constants/index.js` — 预设列表完整
- ✅ `AddNoteSheet.js` — 保存时自动 embed
- ✅ `LearnScreen.js` — 删除时同步删向量

### 阿里云百炼预设新增
- **LLM_PRESETS** 新增 3 个：阿里云 通义千问 Plus/Turbo/Max
  - Base URL: `https://dashscope.aliyuncs.com/compatible-mode/v1`
- **EMBEDDING_PRESETS** 新增 2 个：阿里云 text-embedding-v4 (1024维) / v3 (1536维)
- 删除了不靠谱的 DeepSeek Embedding 预设（DeepSeek 无 embedding API）

### 二次编译
- ✅ BUILD SUCCESSFUL in 23s（缓存命中）
- APK 更新: `D:଀些项目e84 Life OSe84 LifeOSApp
eadme` → 69.9 MB (13:07)

### 待办（更新）
- [ ] APK 传输到手机实测
- [ ] 嵌入模型配置 UI 在设置页确认可用
- [ ] embedding state 展示 UI 提示
- [ ] SettingsScreen 批量 embed 按钮

---

## 2026-07-03 · 学习页 / 习惯规则 / AI 设置重构 / Markdown 渲染（代码完成，APK 未完成）

### 本次目标
围绕 Life OS 的学习记录、习惯打卡、AI 助手和设置页做一轮产品化增强：
- 增加学习/笔记模块；
- 习惯支持按自定义星期生效；
- 打卡页默认只显示今天应该打卡的习惯；
- 设置页从大表单改成折叠式小长条；
- AI API 配置拆成「语言模型」和「嵌入模型」；
- AI 回复支持 Markdown 渲染；
- 为后续日记/学习笔记 RAG 做准备。

### 已完成改动

#### 1. 学习页 / 笔记模块
- 新增 `src/screens/LearnScreen.js`：学习记录、日记/感悟类入口。
- 新增 `src/components/AddNoteSheet.js`：用于新增/编辑学习笔记。
- `App.js` 已注册学习页。
- `TabNav.js` 增加「学习」Tab。
- `AppContext.js` 增加学习笔记相关状态和持久化字段。

#### 2. 习惯自定义星期 + 今日打卡过滤
- `src/utils/helpers.js` 新增：
  - `isHabitActiveOnDate`
  - `isHabitActiveToday`
- `src/components/AddHabitSheet.js` 增加自定义星期 UI。
- `src/screens/HabitScreen.js`：
  - 默认只展示今天应该打卡的习惯；
  - 底部增加「查看全部习惯（N 项）」/「收起 · 只看今天的」按钮；
  - 非今日生效习惯在全部视图中显示「休息」状态，不允许误打卡；
  - 今日无习惯时显示更合理的空状态。

#### 3. 设置页折叠式重构
- `src/screens/SettingsScreen.js` 已重写为 accordion 折叠布局。
- 每个配置项变成一条小长条，显示标题和当前值预览，点击后展开详细配置。
- 区块包括：
  - 语言模型
  - 嵌入模型
  - 待办规则
  - 学期设置
  - 通知设置
  - 数据管理
  - 关于
- 语言模型和嵌入模型分开配置 Base URL / API Key / 模型名。
- 嵌入模型 API Key 默认提示可复用语言模型 Key。

#### 4. 模型预设拆分
- `src/constants/index.js` 新增/调整：
  - `LLM_MODEL_PRESETS` / `LLM_PRESETS`：语言模型预设；
  - `EMBEDDING_MODEL_PRESETS` / `EMBEDDING_PRESETS`：嵌入模型预设；
  - `DEFAULT_SETTINGS` 增加语言模型与嵌入模型字段：
    - `llmApiKey`
    - `llmBaseUrl`
    - `llmModel`
    - `llmPreset`
    - `embeddingApiKey`
    - `embeddingBaseUrl`
    - `embeddingModel`
    - `embeddingPreset`
- 保留旧 `aiApiKey` / `aiBaseUrl` / `aiModel` fallback，避免旧数据直接失效。

#### 5. AI 助手 Markdown 渲染
- `src/components/AIChatSheet.js` 已改造：
  - 引入 `react-native-markdown-display`；
  - 新增 `MsgBubble`，AI 回复优先用 Markdown 渲染，库不可用时 fallback 到纯文本；
  - 增加 Markdown 样式：标题、列表、引用、行内代码、代码块、链接、加粗、斜体、分割线；
  - `getLLMConfig(settings)` 优先读取新的 `llm*` 字段，fallback 到旧 `ai*` 字段；
  - 系统 prompt 明确允许 AI 用 Markdown 回复；
  - 演示模式回复也改成更适合 Markdown 的格式。
- `package.json` 已加入：
  - `react-native-markdown-display`
  - `punycode`（给 `markdown-it` 在 RN Metro 下补 polyfill）

#### 6. AI 工具与课程过滤微调
- `AIChatSheet.js` 的课程查询继续使用 `filterCoursesForDay`，按当前周次、单双周和临时课程过滤。
- 工具调用保留：
  - 查日程 `get_schedule`
  - 完成任务 `complete_task`
  - 创建任务 `create_task`
  - 习惯打卡 `checkin_habit`
  - 查询任务 `query_tasks`

### 构建与验证情况

#### 已通过
- 多个源文件已进行 Babel/语法层面检查，早期修改没有暴露语法错误。
- `react-native-markdown-display` 已在构建目录 `D:\lifeosbuild\LifeOSApp` 安装成功。
- `punycode` 已在构建目录安装成功，解决 Metro 报错：
  - `Unable to resolve module punycode from markdown-it`

#### APK 构建未完成
- 最新一次 Gradle 构建仍失败，位置在 `android/settings.gradle` 的 Expo autolinking 阶段。
- 已确认过一个环境坑：电脑当前 PATH 中的 Node 位于：
  - `D:\apps\New Folder\node.exe`
- 该路径含空格，会导致 Gradle/Expo 通过 `cmd` 调 Node 时出现 255/1 类错误。
- 曾临时复制 Node 到无空格路径：
  - `D:\apps\nodebin\node.exe`
- 但最终构建仍卡在 `settings.gradle` 的 `ex.autolinkLibrariesFromCommand(...)` 附近，未继续深挖。
- 今天不再继续追，下一次从这里接。

### 构建目录与产物状态
- 源码主目录：`D:\一些项目\Life OS\LifeOSApp`
- 构建目录：`D:\lifeosbuild\LifeOSApp`
- 上一版可用 release APK 仍在：
  - `D:\lifeosbuild\LifeOSApp\android\app\build\outputs\apk\release\app-release.apk`
- 注意：该 APK 可能是上一轮成功构建产物，不一定包含本次 21:30 之后的全部改动。

### 重要踩坑记录

#### 1. 中文路径 / 空格路径问题
- 项目主目录含中文和空格：`D:\一些项目\Life OS\LifeOSApp`。
- PowerShell / npm / Gradle 在这个路径下容易出现编码或解析问题。
- 更稳妥做法：
  - 日常源码仍放主目录；
  - 编译使用 `D:\lifeosbuild\LifeOSApp`；
  - npm/Gradle 命令优先用 Git Bash；
  - Node 路径避免空格。

#### 2. react-native-markdown-display 依赖问题
- `react-native-markdown-display` 依赖 `markdown-it`。
- `markdown-it` 会 `require('punycode')`。
- RN Metro 不自动提供 Node 内置模块，因此需要显式安装 `punycode`。

#### 3. 错误创建的 junction
- 曾尝试创建 `D:\apps\nodejs` 指向 Node 目录，但误把目标写成了 `D:\apps\New%20Folder`。
- 该 junction 目标是错误的。
- 删除该 junction 被安全策略拦截；用户已口头确认删除，但本轮结束时未继续处理。
- 下次如果要清理，目标是 junction：
  - `D:\apps\nodejs`
- 需要注意：这是 junction，不是实际 Node 目录；删除 junction 本身不应删除真实 `D:\apps\New Folder`，但仍需谨慎确认。

### 后续待办
- [ ] 清理错误 junction `D:\apps\nodejs`，或新建正确的无空格 Node 路径。
- [ ] 重新跑 `./gradlew assembleRelease --no-daemon`，确认 APK 真正包含本次改动。
- [ ] 若仍卡在 `settings.gradle`，对 Expo autolinking 命令加 `--stacktrace` 查完整原因。
- [ ] 真机检查：
  - 设置页折叠交互；
  - 语言模型 / 嵌入模型配置保存；
  - AI 回复 Markdown 渲染；
  - 今日打卡过滤与查看全部按钮；
  - 学习页新增/编辑笔记。
- [ ] 后续 RAG：基于学习笔记/日记做 embedding，先用云端 embedding + 暴力余弦检索 + 重排序，暂不引入本地向量库。

---

## 2026-06-16 · 服务器推送提醒引擎 + Cloudflare 内网穿透（进行中/未完）

### A. 推送提醒引擎（服务器端已做完并部署，未做真机端到端测试）
- **目标**：本机定时通知在 EMUI 上 App 被杀就失效，想用服务器加强。架构 = App 把
  「下次该提醒的绝对时间点」同步到服务器 → 服务器定时扫描 → 到点**外呼推送中继** →
  推到手机（人在外/ App 关着也能收到）。渠道选定 **Server酱→微信**（微信常驻、送达稳）。
- **server.js 新增**（已部署上线）：
  - 表 `push_config`（每用户存 Server酱 SendKey）、`reminders`（绝对时间点提醒队列）。
  - 路由 `POST /push/config`、`POST /push/test`、`POST /reminders`（整表替换）、`GET /reminders`。
  - `setInterval` 每 30 秒扫描 `reminders`，到点调 Server酱（`sctapi.ftqq.com` 或 `sctp` 开头走 `push.ft07.com`）。
  - 已验证手机能外呼 `sctapi.ftqq.com`（HTTP 200）。
- **待办**：① 用户去 sct.ftqq.com 拿 SendKey → `POST /push/config` + `/push/test` 点亮微信；
  ② App 端把任务DDL/课程/打卡的「下次提醒时间」算出来 POST 到 `/reminders`（要改 App + 重新打包）。

### B. Cloudflare Tunnel 给服务器公网网址（卡住，未完成）
- **目标**：让 App 在任何网络都能连服务器（局域网 IP 外网连不到）。先做免配置的临时隧道(trycloudflare)跑通，再上固定域名。
- **已解决的一串坑**：
  1. GitHub 在手机直连不通 → 用**电脑下载** `cloudflared-linux-arm64` 再 scp 到手机（能跑，v2026.6.0）。
  2. Go 程序读不到系统 DNS（`lookup ... [::1]:53 connection refused`）→ Termux 非 root **绑不了 53 端口**（自写 Node DNS 中继失败）→ 改用 **proot 把 `$PREFIX/etc/resolv.conf` 挂到 `/etc/resolv.conf`**，DNS 通了。
  3. TLS `certificate signed by unknown authority` → 设 `SSL_CERT_FILE=$PREFIX/etc/tls/cert.pem`，已写进 `start-cf.sh`。
- **当前卡点**：proot+cloudflared 跑起来后，**SSH 连手机变成 “banner exchange 超时”**——
  端口还通、ping 也通，但 sshd 来不及握手。判断是**麒麟655 + 宿舍弱 WiFi 被 proot/cloudflared 占满/拖垮**，
  暂时拿不到隧道 URL，也进不去手机。cloudflared 可能还在 nohup 跑着。
- **恢复办法**：手机上重启 Termux（或重启手机）即可让 ssh 恢复；`bash ~/lifeos-server/start.sh` 重起后端。
- **后续判断**：在这台老机 + 弱 WiFi 上常驻跑 cloudflared 较吃力。可选：等 WiFi 稳时重试并盯住负载；
  或改用更轻的 tailscale；或把隧道放到电脑上跑。

### 运维踩坑记录（重要）
- 电脑 `~/.ssh/config` 之前被 PowerShell `utf8` 写入带了 **BOM**，Windows ssh 能忍但 Git Bash 的 ssh 报
  `Bad configuration option: \357\273\277host` → 已用无 BOM 重写。
- 宿舍 WiFi **间歇省电掉线**，ssh 命令时不时 255 → 操作要**带重试循环**；长 `sleep` 的远程命令易撞掉线，
  尽量「启动即返回 + 另起短命令读结果」。

---

## 2026-06-16 · 全周概览布局修复 / 去默认数据 / 导入整表替换

### 1. 「全周概览」柱状图溢出盖住标题
- **现象**：课程表底部「全周概览」的柱子顶到标题上，把「📊 全周概览」遮住。
- **原因**：`weekStats` 容器固定高 80，但柱高写死 `count*16+4`；某天 5 节课就 84px > 80，
  `alignItems:'flex-end'` 让超高部分往上溢出，压到标题。
- **修复**（`CourseScreen.js`）：柱高改成按当周最大节数**等比缩放**（`count/maxCount*56+4`，封顶 60px）；
  `weekStatCol` 改固定 `height:96 + justifyContent:'flex-end'`，容器 `height:96`，内容恒在区域内不外溢。

### 2. 默认不再预置任务与课程
- **需求**：新装/清除数据后不要塞示例任务和课程。
- **改动**（`AppContext.js`）：`seedTasks`、`seedCourses` 置空 `[]`（习惯 seedHabits 暂保留，未要求动）。
- 注意：**已安装的机器**里旧的示例数据仍在本地存储，需手动删除或「设置→清除数据」，或直接靠下面的导入整表替换冲掉。

### 3. 导入课程表 = 整表替换 + AI 提示
- **需求**：截图导入课程表时自动删除以前的课程表，并在 AI 回复里说明。
- **改动**：
  - reducer 新增 `REPLACE_COURSES`（清空旧课程换成传入的）；`App.js` 的 `handleImportCourses`
    由 `ADD_COURSES` 改为 `REPLACE_COURSES`。
  - `AIChatSheet.js` 导入成功回复前面加一句：`🔄 已清空原有 N 门课程，并替换为本次导入的内容。`
    （N 取导入前的 `courses.length`，为 0 时不显示这句）。

### 验证
- `npx expo export -p web` 通过（exit 0，导出 dist）。

---

## 2026-06-16 · 截图导入耗时提示 + 视觉模型踩坑

### 踩坑：截图导入必须用「视觉/多模态」模型
- **现象**：配了小米 `mimo-v2.5`，上传课程表截图老提示「图片不清晰」，图再清晰也没用。
- **真因**：`mimo-v2.5` 是**纯文本模型**，根本看不到图（能看图的是小米 **MiMo-VL** 那个 VL 分支）。
  模型收到图回了句自然语言（大意"看不清/没有图"），而 `parseCoursesJson` 解析非 JSON 失败后
  抛的是「无法解析识别结果，请换一张更清晰的截图」——**把模型真实回复吞掉了**，误导成图片问题。
- **结论/选型**：截图导入必须用支持图片的模型（小米 MiMo-VL / gpt-4o / gpt-4o-mini 等）。
  换成视觉模型后识别成功。
- TODO（未做）：`extractCoursesFromImage` 解析失败时把模型原始回复也带出来，方便排查。

### 改动：识别耗时较长的提示
- **需求**：视觉模型识别慢，过程中怕用户以为卡死。
- **方案**：`AIChatSheet.js` 加 `importSecs` 每秒计时，识别中的 typing 提示随时间升级：
  0–8s「识别课程表中…」→ 8–20s「视觉模型较慢，请再稍候（已 Ns）」→ 20s+「仍在识别，图越大越慢，请耐心等待（已 Ns）」，带实时秒数。
  `importing` 结束自动清零（useEffect 起/停 interval）。

### 验证
- `npx expo export -p web` 通过（exit 0，导出 dist）。

---

## 2026-06-16 · 云端同步后端（自建测试服务器，PRD 5.7）

补上 PRD 第 5.7 节一直没做的**云端同步后端**。用一台旧手机跑 Termux 当临时测试服务器，
后端代码在仓库外的 `D:\一些项目\Life OS\server\`（server.js / package.json / start.sh）。

### 技术选型（受手机硬件约束：麒麟655 / 3GB内存 / ~8GB可用 / 能上外网无VPN）
- **后端 Node.js**（跟 App 同语言，轻）。
- **数据库 SQLite**，不用 PostgreSQL（3GB 内存扛不住）。
- **纯 Node 内置模块、零外部依赖、零编译**：`http` + `node:sqlite` + `node:crypto`。
  手机 Node v24 自带 `node:sqlite` 开箱即用，避免在 Termux 上编译 npm 原生模块。
  ⚠️ 坑：`DatabaseSync` 没有 better-sqlite3 的 `.transaction()`，要手动 `BEGIN/COMMIT/ROLLBACK`。

### 设计
- **鉴权**：邮箱+密码，`scrypt` 加盐哈希，登录发 token（Bearer）。
- **通用同步存储**：一张 `records(userId,collection,id,data,updatedAt,isDeleted,serverSeq)`
  表装下所有实体（courses/tasks/habits/打卡记录/plans/settings），App 以后加实体不用改后端。
- **增量同步**：服务器给每次写入分配全局单调递增 `serverSeq`，客户端存「上次拉到的 cursor」
  当下次 `since`——避开手机/服务器时钟不一致。冲突按 `updatedAt` 比大小 **last-write-wins**；
  删除为软删除（`isDeleted=1`，仍参与同步）。

### API（基地址 `http://192.168.10.234:3000`）
```
POST /auth/register {email,password}            -> {token, userId}
POST /auth/login    {email,password}            -> {token, userId}
POST /sync  (Header: Authorization: Bearer <token>)
     body {since:<cursor>, changes:[{collection,id,data,updatedAt,isDeleted}]}
     resp {serverTime, cursor, changes:[...]}
GET  /health
```

### 🔌 服务器关键信息 / 怎么控制（⚠️宿舍停电后看这里）
- **手机服务器**：LAN IP `192.168.10.234`，SSH 端口 `8022`，用户 `u0_a136`。
- **从电脑控制**：已配免密 SSH，直接 `ssh lifeos-phone` 进（别名在电脑 `~/.ssh/config`）。
- **手机上后端目录**：`~/lifeos-server/`（server.js / start.sh / lifeos.db）。
- **⚡停电/重启后恢复服务的方法**（手机来电、连回 WiFi 后，在电脑上执行一句）：
  ```
  ssh lifeos-phone "bash ~/lifeos-server/start.sh"
  ```
  `start.sh` 会自动：开 `termux-wake-lock` 防休眠 → 杀掉旧进程 → `nohup` 后台重启 →
  打印日志和 RUNNING 状态。看到 `RUNNING pid=...` 即恢复。
- **没电脑时直接在手机 Termux 里**：`bash ~/lifeos-server/start.sh` 同样可重启。
- **健康检查**：电脑上 `curl --noproxy "*" http://192.168.10.234:3000/health` 返回 `{"ok":true,...}`。
- ⚠️ 当前是 nohup 后台，**ssh 断开不受影响，但手机重启不会自启**——停电后必须手动跑 start.sh。
  （后续可装 `termux-boot` 实现开机自启，免去手动。）
- ⚠️ 运维注意：PowerShell→ssh 跑内联 `node -e` 脚本，引号/中文必坏 → 用 base64 传脚本再跑；
  访问后端要 `curl --noproxy "*"` 绕开本机 M78Accelerator 代理。

### 验证（电脑端 curl/Invoke-RestMethod 全程跑通）
- 注册发 token ✓；推送→拉回 ✓；
- last-write-wins：旧 updatedAt 不覆盖 ✓、新 updatedAt 覆盖 ✓；
- 软删除同步 ✓；增量游标只回拉变化记录 ✓；
- 中文 UTF-8 字节无损 ✓（在手机端按字节比对 `e9ab98e695b0`=「高数」）。
  注：用 PowerShell `Invoke-RestMethod` 发含中文的 body 会被编码成 `??`，那是测试客户端的锅，
  不是服务器问题——RN App 用 `fetch` 发标准 UTF-8 无此问题，已用 `curl --data-binary` 证实。

### 待办 / 后续
- [ ] 把 RN App 接上来：加同步逻辑（离线优先：先写本地→后台增量同步）+ 设置页填「服务器地址 / 账号」。
- [ ] 可选：手机装 `termux-boot`，停电来电后开机自启后端，免手动 start.sh。
- [ ] 生产化前再做：HTTPS、token 过期、限流（当前是测试服务器，暂不做）。

---

## 2026-06-14 · 七计划（替换待办页「列表」视图）

把待办页里原来只是平铺四象限任务的「列表」视图，换成全新的 **七计划**。

### 概念
- 仿照中国「一五计划 / 十四五」的命名，**每七天（一周）= 一个计划单元**：
  一七计划、二七计划、三七计划……
- 由用户**每周日或周一自己手写**，作为：
  - 长期任务的「最小解决单元」（把大目标拆成这一周能完成的一小步）
  - 紧急任务的「本周安排」
- 不是自动从四象限搬运，而是用户自己写。

### 设计决策（与用户确认）
- **编号**：从首次使用那周连续累加（不绑学期）。锚点 `settings.planAnchorMonday`
  存「最早有计划的那周周一」，序号 = (本周 - 锚点)/7 + 1，再转中文。
- **计划项**：手写为主，可选「关联」一个进行中的任务（带象限/DDL 提示），
  关联后再补一句「这周推进到哪一步」。
- **位置**：替换待办页顶部 `[📊 四象限] [🗓 七计划]` 切换里的「列表」。

### 实现
- 数据：`plans[mondayKey] = { longItems, urgentItems }`，item = `{id,text,done,linkedTaskId}`。
  storage key `lifeos.plans.v1`；reducer 加 `SET_PLANS / ADD/TOGGLE/UPDATE/DELETE_PLAN_ITEM`。
- helpers 加 `numToChinese / mondayKey / planOrdinal / planTitle / weekRangeLabel / shiftDays`。
- 新组件 `src/components/WeekPlanView.js`：周左右切换（‹ › + 回到本周）、
  周日/周一本周空计划时顶部提醒、两栏手写清单 + 任务关联选择器。
- `TodoScreen.js`：`viewMode` 'list' → 'plan'，渲染 `WeekPlanView`；
  摘要条/象限提示/已完成列表仅在四象限视图显示；删掉废弃的 `ListView`。

### 验证
- `npx expo export -p web` 通过（384 modules，exit 0）。
- Playwright 390×844：切到七计划显示「一七计划 / 二七计划」连续编号、日期范围、
  两栏齐全；手写添加✓、勾选完成✓、🔗关联任务弹选择器并显示 chip✓、
  ‹ › 切周 + 「回到本周」✓；控制台 0 报错。

---

## 2026-06-14 · 临时课程 / 截图导入课程表 / AI 按钮可拖拽

本次新增三个功能。

### 1. 临时课程（调课、补课只上一次）
- **需求**：课程表会临时加课，原来只能加「按周重复」的课。
- **方案**：课程数据新增 `temporary`（布尔）和 `date`（YYYY-MM-DD）字段。
  - `AddCourseSheet` 加「临时课程」开关；打开后隐藏「星期 / 起止周 / 单双周」，
    改成横向滚动的「未来 14 天」日期选择。
  - 新增 helper：`startOfWeek` / `isSameWeek` / `weekdayOf` / `isCourseOnDay`。
    临时课只在它指定那天、且属于「本周」时显示在对应星期下。
  - 课程卡片显示「临时」橙色徽标和具体日期。

### 2. AI 助手：截图导入课程表
- **需求**：把教务网课程表截图丢给 AI，自动生成课程。
- **方案**：
  - 新增依赖 `expo-image-picker`（原生模块，⚠️ 下次打包需重新 prebuild）。
  - `AIChatSheet` 输入框左侧加 📷 按钮：选图 → 转 base64 data URL →
    调用视觉模型（OpenAI 兼容 `/chat/completions` 的 `image_url`）→
    严格 JSON 输出 → 容错解析（去代码块、截取数组、字段校验）→
    批量导入。需要支持图片的模型（如 gpt-4o / gpt-4o-mini）。
  - reducer 新增 `ADD_COURSES` 批量添加；导入后在对话里给出预览清单。

### 3. AI 浮动按钮可拖拽
- **需求**：看「已完成任务」时 AI 按钮挡住了内容。
- **方案**：`App.js` 新增 `DraggableAIButton`，用内置 `PanResponder + Animated`
  （无新依赖）。拖动改位置、轻点打开对话（位移 < 4px 判定为点击）；
  位置夹在屏幕安全区内，落点存到 `settings.aiFabPos`，重启后保持。

### 涉及文件
- **新增依赖**：`expo-image-picker`
- **修改**：`App.js`、`src/store/AppContext.js`、`src/utils/helpers.js`、
  `src/components/AddCourseSheet.js`、`src/components/AIChatSheet.js`、
  `src/screens/CourseScreen.js`

### 验证
- `npx expo export -p web` 通过（383 modules，exit 0）。
- Playwright 390×844 移动视口：临时课程开关→切出「上课日期」正常；
  📷 按钮渲染正常；FAB 拖拽从 (328,710) 移到 (208,550) 且拖拽不误触打开 AI；
  控制台 0 报错。
- ⚠️ 截图导入的完整链路（相册权限 + 视觉模型识别）需在**真机 + 配好支持图片的
  API Key** 后实测；Web 无法验证相册与视觉调用。
- ⚠️ 因新增原生模块 `expo-image-picker`，重新打包前需 `expo prebuild`
  （记得复制到英文路径再 prebuild/assembleRelease，中文路径会挂）。

---

## 2026-06-13 · 体验优化（输入键盘 / 详情合并 / 提示弹窗）

本次针对三个使用体验问题做了修复。

### 1. 手机上输入框被键盘遮挡（要「闭眼打字」）
- **现象**：在底部弹窗里输入时，键盘弹出后输入框不会上移，被键盘挡住。
- **原因**：所有底部弹窗用的是
  `behavior={Platform.OS === 'ios' ? 'padding' : undefined}`，
  即 **Android 上 `KeyboardAvoidingView` 等于没生效**；
  而且 RN 的 `<Modal>` 在 Android 是独立 window，
  系统的 `adjustResize` 和原生 KeyboardAvoidingView 都够不到弹窗内部。
- **方案**：新增通用组件 `KeyboardAvoidingSheet`，
  直接监听键盘事件（iOS `keyboardWillShow/Hide`、Android `keyboardDidShow/Hide`），
  把弹窗整体抬到键盘上方，配合 `LayoutAnimation` 平滑过渡。
  替换了全部 5 个弹窗：新建任务 / 任务详情 / 习惯 / 课程 / AI 对话。

### 2. 「实现步骤」和「设置」分离，来回切不方便
- **现象**：打开任务看到的是步骤清单，要改截止日期/紧急/备注却要点「编辑」
  跳到**另一个弹窗**（AddTaskSheet），两个面板来回跳。
- **方案**：把 `TaskDetailSheet` 重写成**一个统一的编辑面板** ——
  标题、截止日期、紧急开关、备注 与 实现步骤清单 全部在同一个弹窗里，
  改动即时保存，象限徽标 / DDL 文案随设置实时刷新。
  去掉了「编辑→另开弹窗」的跳转；`AddTaskSheet` 只保留给**新建**任务用。

### 3. 删除成功等提示弹窗 UI 太老
- **现象**：用的是系统原生 `Alert.alert`，样式陈旧。
- **方案**：新增全局反馈 `FeedbackProvider`（`useFeedback()`），用现代组件替换：
  - **Toast**：顶部滑入的深色胶囊提示。删除任务时显示「已删除任务 · **撤销**」，
    一键撤销（任务是软删除，可恢复）。
  - **ConfirmDialog**：圆角卡片确认框，用于不可逆操作
    （删除课程 / 删除习惯 / 清除所有数据）。
  - 设置页的「测试连接」「清除数据」结果也改成 Toast。

### 涉及文件
- **新增**：
  - `src/components/KeyboardAvoidingSheet.js`
  - `src/components/Toast.js`
  - `src/components/ConfirmDialog.js`
  - `src/store/FeedbackContext.js`
- **修改**：
  - `App.js`（套上 FeedbackProvider）
  - `src/components/AddTaskSheet.js`、`AIChatSheet.js`、`AddHabitSheet.js`、`AddCourseSheet.js`（接入 KeyboardAvoidingSheet）
  - `src/components/TaskDetailSheet.js`（重写为统一编辑面板）
  - `src/screens/TodoScreen.js`（统一编辑接线 + 删除撤销 Toast，移除编辑跳转）
  - `src/screens/CourseScreen.js`、`HabitScreen.js`、`SettingsScreen.js`（Alert → confirm/toast）

### 验证
- `npx expo export -p web` 打包通过（376 modules，exit 0）。
- 用 Playwright 在 390×844 移动视口跑通真机交互：统一详情面板、删除撤销 Toast、
  确认弹窗均正常渲染并工作；改 DDL 时「剩余 2 天 → 明天截止」实时刷新。
- ⚠️ 键盘抬升只能在真机上体验（Web 无软键盘），需要打包后在手机上验证。

### 待办 / 后续
- [ ] 在手机上实测键盘抬升效果（重点：Android）。
- [ ] 需要独立安装到手机验证时，记得用 `assembleRelease` 重新打包
      （debug 包是空壳，装上会红屏）。
